// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	uuid "github.com/gofrs/uuid/v5"
	"github.com/rj-davidson/greenrats/ent/syncstatus"
)

// SyncStatus is the model entity for the SyncStatus schema.
type SyncStatus struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// SyncType holds the value of the "sync_type" field.
	SyncType string `json:"sync_type,omitempty"`
	// LastSyncAt holds the value of the "last_sync_at" field.
	LastSyncAt   time.Time `json:"last_sync_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SyncStatus) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case syncstatus.FieldSyncType:
			values[i] = new(sql.NullString)
		case syncstatus.FieldCreatedAt, syncstatus.FieldUpdatedAt, syncstatus.FieldLastSyncAt:
			values[i] = new(sql.NullTime)
		case syncstatus.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SyncStatus fields.
func (_m *SyncStatus) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case syncstatus.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case syncstatus.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case syncstatus.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case syncstatus.FieldSyncType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sync_type", values[i])
			} else if value.Valid {
				_m.SyncType = value.String
			}
		case syncstatus.FieldLastSyncAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_sync_at", values[i])
			} else if value.Valid {
				_m.LastSyncAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SyncStatus.
// This includes values selected through modifiers, order, etc.
func (_m *SyncStatus) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this SyncStatus.
// Note that you need to call SyncStatus.Unwrap() before calling this method if this SyncStatus
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SyncStatus) Update() *SyncStatusUpdateOne {
	return NewSyncStatusClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SyncStatus entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SyncStatus) Unwrap() *SyncStatus {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SyncStatus is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SyncStatus) String() string {
	var builder strings.Builder
	builder.WriteString("SyncStatus(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("sync_type=")
	builder.WriteString(_m.SyncType)
	builder.WriteString(", ")
	builder.WriteString("last_sync_at=")
	builder.WriteString(_m.LastSyncAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SyncStatusSlice is a parsable slice of SyncStatus.
type SyncStatusSlice []*SyncStatus
