// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	uuid "github.com/gofrs/uuid/v5"
	"github.com/rj-davidson/greenrats/ent/course"
	"github.com/rj-davidson/greenrats/ent/tournament"
	"github.com/rj-davidson/greenrats/ent/tournamentcourse"
)

// TournamentCourse is the model entity for the TournamentCourse schema.
type TournamentCourse struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Which round numbers use this course (e.g., [1,2] or [1,2,3,4])
	Rounds []int `json:"rounds,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TournamentCourseQuery when eager-loading is set.
	Edges                         TournamentCourseEdges `json:"edges"`
	course_tournament_courses     *uuid.UUID
	tournament_tournament_courses *uuid.UUID
	selectValues                  sql.SelectValues
}

// TournamentCourseEdges holds the relations/edges for other nodes in the graph.
type TournamentCourseEdges struct {
	// Tournament holds the value of the tournament edge.
	Tournament *Tournament `json:"tournament,omitempty"`
	// Course holds the value of the course edge.
	Course *Course `json:"course,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TournamentOrErr returns the Tournament value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TournamentCourseEdges) TournamentOrErr() (*Tournament, error) {
	if e.Tournament != nil {
		return e.Tournament, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tournament.Label}
	}
	return nil, &NotLoadedError{edge: "tournament"}
}

// CourseOrErr returns the Course value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TournamentCourseEdges) CourseOrErr() (*Course, error) {
	if e.Course != nil {
		return e.Course, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: course.Label}
	}
	return nil, &NotLoadedError{edge: "course"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TournamentCourse) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tournamentcourse.FieldRounds:
			values[i] = new([]byte)
		case tournamentcourse.FieldCreatedAt, tournamentcourse.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case tournamentcourse.FieldID:
			values[i] = new(uuid.UUID)
		case tournamentcourse.ForeignKeys[0]: // course_tournament_courses
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case tournamentcourse.ForeignKeys[1]: // tournament_tournament_courses
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TournamentCourse fields.
func (_m *TournamentCourse) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tournamentcourse.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case tournamentcourse.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case tournamentcourse.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case tournamentcourse.FieldRounds:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field rounds", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Rounds); err != nil {
					return fmt.Errorf("unmarshal field rounds: %w", err)
				}
			}
		case tournamentcourse.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field course_tournament_courses", values[i])
			} else if value.Valid {
				_m.course_tournament_courses = new(uuid.UUID)
				*_m.course_tournament_courses = *value.S.(*uuid.UUID)
			}
		case tournamentcourse.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field tournament_tournament_courses", values[i])
			} else if value.Valid {
				_m.tournament_tournament_courses = new(uuid.UUID)
				*_m.tournament_tournament_courses = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TournamentCourse.
// This includes values selected through modifiers, order, etc.
func (_m *TournamentCourse) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTournament queries the "tournament" edge of the TournamentCourse entity.
func (_m *TournamentCourse) QueryTournament() *TournamentQuery {
	return NewTournamentCourseClient(_m.config).QueryTournament(_m)
}

// QueryCourse queries the "course" edge of the TournamentCourse entity.
func (_m *TournamentCourse) QueryCourse() *CourseQuery {
	return NewTournamentCourseClient(_m.config).QueryCourse(_m)
}

// Update returns a builder for updating this TournamentCourse.
// Note that you need to call TournamentCourse.Unwrap() before calling this method if this TournamentCourse
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TournamentCourse) Update() *TournamentCourseUpdateOne {
	return NewTournamentCourseClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TournamentCourse entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TournamentCourse) Unwrap() *TournamentCourse {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TournamentCourse is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TournamentCourse) String() string {
	var builder strings.Builder
	builder.WriteString("TournamentCourse(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("rounds=")
	builder.WriteString(fmt.Sprintf("%v", _m.Rounds))
	builder.WriteByte(')')
	return builder.String()
}

// TournamentCourses is a parsable slice of TournamentCourse.
type TournamentCourses []*TournamentCourse
