// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	uuid "github.com/gofrs/uuid/v5"
	"github.com/rj-davidson/greenrats/ent/emailreminder"
	"github.com/rj-davidson/greenrats/ent/league"
	"github.com/rj-davidson/greenrats/ent/tournament"
	"github.com/rj-davidson/greenrats/ent/user"
)

// EmailReminder is the model entity for the EmailReminder schema.
type EmailReminder struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ReminderType holds the value of the "reminder_type" field.
	ReminderType emailreminder.ReminderType `json:"reminder_type,omitempty"`
	// SentAt holds the value of the "sent_at" field.
	SentAt time.Time `json:"sent_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EmailReminderQuery when eager-loading is set.
	Edges                      EmailReminderEdges `json:"edges"`
	league_email_reminders     *uuid.UUID
	tournament_email_reminders *uuid.UUID
	user_email_reminders       *uuid.UUID
	selectValues               sql.SelectValues
}

// EmailReminderEdges holds the relations/edges for other nodes in the graph.
type EmailReminderEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Tournament holds the value of the tournament edge.
	Tournament *Tournament `json:"tournament,omitempty"`
	// League holds the value of the league edge.
	League *League `json:"league,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EmailReminderEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// TournamentOrErr returns the Tournament value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EmailReminderEdges) TournamentOrErr() (*Tournament, error) {
	if e.Tournament != nil {
		return e.Tournament, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: tournament.Label}
	}
	return nil, &NotLoadedError{edge: "tournament"}
}

// LeagueOrErr returns the League value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EmailReminderEdges) LeagueOrErr() (*League, error) {
	if e.League != nil {
		return e.League, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: league.Label}
	}
	return nil, &NotLoadedError{edge: "league"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EmailReminder) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case emailreminder.FieldReminderType:
			values[i] = new(sql.NullString)
		case emailreminder.FieldSentAt:
			values[i] = new(sql.NullTime)
		case emailreminder.FieldID:
			values[i] = new(uuid.UUID)
		case emailreminder.ForeignKeys[0]: // league_email_reminders
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case emailreminder.ForeignKeys[1]: // tournament_email_reminders
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case emailreminder.ForeignKeys[2]: // user_email_reminders
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EmailReminder fields.
func (_m *EmailReminder) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case emailreminder.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case emailreminder.FieldReminderType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field reminder_type", values[i])
			} else if value.Valid {
				_m.ReminderType = emailreminder.ReminderType(value.String)
			}
		case emailreminder.FieldSentAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field sent_at", values[i])
			} else if value.Valid {
				_m.SentAt = value.Time
			}
		case emailreminder.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field league_email_reminders", values[i])
			} else if value.Valid {
				_m.league_email_reminders = new(uuid.UUID)
				*_m.league_email_reminders = *value.S.(*uuid.UUID)
			}
		case emailreminder.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field tournament_email_reminders", values[i])
			} else if value.Valid {
				_m.tournament_email_reminders = new(uuid.UUID)
				*_m.tournament_email_reminders = *value.S.(*uuid.UUID)
			}
		case emailreminder.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_email_reminders", values[i])
			} else if value.Valid {
				_m.user_email_reminders = new(uuid.UUID)
				*_m.user_email_reminders = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EmailReminder.
// This includes values selected through modifiers, order, etc.
func (_m *EmailReminder) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the EmailReminder entity.
func (_m *EmailReminder) QueryUser() *UserQuery {
	return NewEmailReminderClient(_m.config).QueryUser(_m)
}

// QueryTournament queries the "tournament" edge of the EmailReminder entity.
func (_m *EmailReminder) QueryTournament() *TournamentQuery {
	return NewEmailReminderClient(_m.config).QueryTournament(_m)
}

// QueryLeague queries the "league" edge of the EmailReminder entity.
func (_m *EmailReminder) QueryLeague() *LeagueQuery {
	return NewEmailReminderClient(_m.config).QueryLeague(_m)
}

// Update returns a builder for updating this EmailReminder.
// Note that you need to call EmailReminder.Unwrap() before calling this method if this EmailReminder
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EmailReminder) Update() *EmailReminderUpdateOne {
	return NewEmailReminderClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EmailReminder entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EmailReminder) Unwrap() *EmailReminder {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: EmailReminder is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EmailReminder) String() string {
	var builder strings.Builder
	builder.WriteString("EmailReminder(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("reminder_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.ReminderType))
	builder.WriteString(", ")
	builder.WriteString("sent_at=")
	builder.WriteString(_m.SentAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EmailReminders is a parsable slice of EmailReminder.
type EmailReminders []*EmailReminder
