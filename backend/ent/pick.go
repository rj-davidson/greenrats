// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	uuid "github.com/gofrs/uuid/v5"
	"github.com/rj-davidson/greenrats/ent/golfer"
	"github.com/rj-davidson/greenrats/ent/league"
	"github.com/rj-davidson/greenrats/ent/pick"
	"github.com/rj-davidson/greenrats/ent/tournament"
	"github.com/rj-davidson/greenrats/ent/user"
)

// Pick is the model entity for the Pick schema.
type Pick struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// SeasonYear holds the value of the "season_year" field.
	SeasonYear int `json:"season_year,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PickQuery when eager-loading is set.
	Edges            PickEdges `json:"edges"`
	golfer_picks     *uuid.UUID
	league_picks     *uuid.UUID
	tournament_picks *uuid.UUID
	user_picks       *uuid.UUID
	selectValues     sql.SelectValues
}

// PickEdges holds the relations/edges for other nodes in the graph.
type PickEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Tournament holds the value of the tournament edge.
	Tournament *Tournament `json:"tournament,omitempty"`
	// Golfer holds the value of the golfer edge.
	Golfer *Golfer `json:"golfer,omitempty"`
	// League holds the value of the league edge.
	League *League `json:"league,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PickEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// TournamentOrErr returns the Tournament value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PickEdges) TournamentOrErr() (*Tournament, error) {
	if e.Tournament != nil {
		return e.Tournament, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: tournament.Label}
	}
	return nil, &NotLoadedError{edge: "tournament"}
}

// GolferOrErr returns the Golfer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PickEdges) GolferOrErr() (*Golfer, error) {
	if e.Golfer != nil {
		return e.Golfer, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: golfer.Label}
	}
	return nil, &NotLoadedError{edge: "golfer"}
}

// LeagueOrErr returns the League value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PickEdges) LeagueOrErr() (*League, error) {
	if e.League != nil {
		return e.League, nil
	} else if e.loadedTypes[3] {
		return nil, &NotFoundError{label: league.Label}
	}
	return nil, &NotLoadedError{edge: "league"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Pick) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pick.FieldSeasonYear:
			values[i] = new(sql.NullInt64)
		case pick.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case pick.FieldID:
			values[i] = new(uuid.UUID)
		case pick.ForeignKeys[0]: // golfer_picks
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case pick.ForeignKeys[1]: // league_picks
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case pick.ForeignKeys[2]: // tournament_picks
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case pick.ForeignKeys[3]: // user_picks
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Pick fields.
func (_m *Pick) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pick.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case pick.FieldSeasonYear:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field season_year", values[i])
			} else if value.Valid {
				_m.SeasonYear = int(value.Int64)
			}
		case pick.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case pick.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field golfer_picks", values[i])
			} else if value.Valid {
				_m.golfer_picks = new(uuid.UUID)
				*_m.golfer_picks = *value.S.(*uuid.UUID)
			}
		case pick.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field league_picks", values[i])
			} else if value.Valid {
				_m.league_picks = new(uuid.UUID)
				*_m.league_picks = *value.S.(*uuid.UUID)
			}
		case pick.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field tournament_picks", values[i])
			} else if value.Valid {
				_m.tournament_picks = new(uuid.UUID)
				*_m.tournament_picks = *value.S.(*uuid.UUID)
			}
		case pick.ForeignKeys[3]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_picks", values[i])
			} else if value.Valid {
				_m.user_picks = new(uuid.UUID)
				*_m.user_picks = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Pick.
// This includes values selected through modifiers, order, etc.
func (_m *Pick) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Pick entity.
func (_m *Pick) QueryUser() *UserQuery {
	return NewPickClient(_m.config).QueryUser(_m)
}

// QueryTournament queries the "tournament" edge of the Pick entity.
func (_m *Pick) QueryTournament() *TournamentQuery {
	return NewPickClient(_m.config).QueryTournament(_m)
}

// QueryGolfer queries the "golfer" edge of the Pick entity.
func (_m *Pick) QueryGolfer() *GolferQuery {
	return NewPickClient(_m.config).QueryGolfer(_m)
}

// QueryLeague queries the "league" edge of the Pick entity.
func (_m *Pick) QueryLeague() *LeagueQuery {
	return NewPickClient(_m.config).QueryLeague(_m)
}

// Update returns a builder for updating this Pick.
// Note that you need to call Pick.Unwrap() before calling this method if this Pick
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Pick) Update() *PickUpdateOne {
	return NewPickClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Pick entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Pick) Unwrap() *Pick {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Pick is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Pick) String() string {
	var builder strings.Builder
	builder.WriteString("Pick(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("season_year=")
	builder.WriteString(fmt.Sprintf("%v", _m.SeasonYear))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Picks is a parsable slice of Pick.
type Picks []*Pick
