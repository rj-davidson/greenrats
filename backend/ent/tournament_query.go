// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	uuid "github.com/gofrs/uuid/v5"
	"github.com/rj-davidson/greenrats/ent/emailreminder"
	"github.com/rj-davidson/greenrats/ent/pick"
	"github.com/rj-davidson/greenrats/ent/predicate"
	"github.com/rj-davidson/greenrats/ent/tournament"
	"github.com/rj-davidson/greenrats/ent/tournamententry"
)

// TournamentQuery is the builder for querying Tournament entities.
type TournamentQuery struct {
	config
	ctx                *QueryContext
	order              []tournament.OrderOption
	inters             []Interceptor
	predicates         []predicate.Tournament
	withPicks          *PickQuery
	withEntries        *TournamentEntryQuery
	withEmailReminders *EmailReminderQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TournamentQuery builder.
func (_q *TournamentQuery) Where(ps ...predicate.Tournament) *TournamentQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *TournamentQuery) Limit(limit int) *TournamentQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *TournamentQuery) Offset(offset int) *TournamentQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *TournamentQuery) Unique(unique bool) *TournamentQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *TournamentQuery) Order(o ...tournament.OrderOption) *TournamentQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryPicks chains the current query on the "picks" edge.
func (_q *TournamentQuery) QueryPicks() *PickQuery {
	query := (&PickClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tournament.Table, tournament.FieldID, selector),
			sqlgraph.To(pick.Table, pick.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, tournament.PicksTable, tournament.PicksColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEntries chains the current query on the "entries" edge.
func (_q *TournamentQuery) QueryEntries() *TournamentEntryQuery {
	query := (&TournamentEntryClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tournament.Table, tournament.FieldID, selector),
			sqlgraph.To(tournamententry.Table, tournamententry.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, tournament.EntriesTable, tournament.EntriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEmailReminders chains the current query on the "email_reminders" edge.
func (_q *TournamentQuery) QueryEmailReminders() *EmailReminderQuery {
	query := (&EmailReminderClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tournament.Table, tournament.FieldID, selector),
			sqlgraph.To(emailreminder.Table, emailreminder.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, tournament.EmailRemindersTable, tournament.EmailRemindersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Tournament entity from the query.
// Returns a *NotFoundError when no Tournament was found.
func (_q *TournamentQuery) First(ctx context.Context) (*Tournament, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tournament.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *TournamentQuery) FirstX(ctx context.Context) *Tournament {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Tournament ID from the query.
// Returns a *NotFoundError when no Tournament ID was found.
func (_q *TournamentQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tournament.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *TournamentQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Tournament entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Tournament entity is found.
// Returns a *NotFoundError when no Tournament entities are found.
func (_q *TournamentQuery) Only(ctx context.Context) (*Tournament, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tournament.Label}
	default:
		return nil, &NotSingularError{tournament.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *TournamentQuery) OnlyX(ctx context.Context) *Tournament {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Tournament ID in the query.
// Returns a *NotSingularError when more than one Tournament ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *TournamentQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tournament.Label}
	default:
		err = &NotSingularError{tournament.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *TournamentQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Tournaments.
func (_q *TournamentQuery) All(ctx context.Context) ([]*Tournament, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Tournament, *TournamentQuery]()
	return withInterceptors[[]*Tournament](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *TournamentQuery) AllX(ctx context.Context) []*Tournament {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Tournament IDs.
func (_q *TournamentQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(tournament.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *TournamentQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *TournamentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*TournamentQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *TournamentQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *TournamentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *TournamentQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TournamentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *TournamentQuery) Clone() *TournamentQuery {
	if _q == nil {
		return nil
	}
	return &TournamentQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]tournament.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.Tournament{}, _q.predicates...),
		withPicks:          _q.withPicks.Clone(),
		withEntries:        _q.withEntries.Clone(),
		withEmailReminders: _q.withEmailReminders.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithPicks tells the query-builder to eager-load the nodes that are connected to
// the "picks" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TournamentQuery) WithPicks(opts ...func(*PickQuery)) *TournamentQuery {
	query := (&PickClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPicks = query
	return _q
}

// WithEntries tells the query-builder to eager-load the nodes that are connected to
// the "entries" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TournamentQuery) WithEntries(opts ...func(*TournamentEntryQuery)) *TournamentQuery {
	query := (&TournamentEntryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEntries = query
	return _q
}

// WithEmailReminders tells the query-builder to eager-load the nodes that are connected to
// the "email_reminders" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TournamentQuery) WithEmailReminders(opts ...func(*EmailReminderQuery)) *TournamentQuery {
	query := (&EmailReminderClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEmailReminders = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Tournament.Query().
//		GroupBy(tournament.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *TournamentQuery) GroupBy(field string, fields ...string) *TournamentGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TournamentGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = tournament.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Tournament.Query().
//		Select(tournament.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *TournamentQuery) Select(fields ...string) *TournamentSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &TournamentSelect{TournamentQuery: _q}
	sbuild.label = tournament.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TournamentSelect configured with the given aggregations.
func (_q *TournamentQuery) Aggregate(fns ...AggregateFunc) *TournamentSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *TournamentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !tournament.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *TournamentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Tournament, error) {
	var (
		nodes       = []*Tournament{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withPicks != nil,
			_q.withEntries != nil,
			_q.withEmailReminders != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Tournament).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Tournament{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withPicks; query != nil {
		if err := _q.loadPicks(ctx, query, nodes,
			func(n *Tournament) { n.Edges.Picks = []*Pick{} },
			func(n *Tournament, e *Pick) { n.Edges.Picks = append(n.Edges.Picks, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEntries; query != nil {
		if err := _q.loadEntries(ctx, query, nodes,
			func(n *Tournament) { n.Edges.Entries = []*TournamentEntry{} },
			func(n *Tournament, e *TournamentEntry) { n.Edges.Entries = append(n.Edges.Entries, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEmailReminders; query != nil {
		if err := _q.loadEmailReminders(ctx, query, nodes,
			func(n *Tournament) { n.Edges.EmailReminders = []*EmailReminder{} },
			func(n *Tournament, e *EmailReminder) { n.Edges.EmailReminders = append(n.Edges.EmailReminders, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *TournamentQuery) loadPicks(ctx context.Context, query *PickQuery, nodes []*Tournament, init func(*Tournament), assign func(*Tournament, *Pick)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Tournament)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Pick(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(tournament.PicksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.tournament_picks
		if fk == nil {
			return fmt.Errorf(`foreign-key "tournament_picks" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "tournament_picks" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *TournamentQuery) loadEntries(ctx context.Context, query *TournamentEntryQuery, nodes []*Tournament, init func(*Tournament), assign func(*Tournament, *TournamentEntry)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Tournament)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.TournamentEntry(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(tournament.EntriesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.tournament_entries
		if fk == nil {
			return fmt.Errorf(`foreign-key "tournament_entries" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "tournament_entries" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *TournamentQuery) loadEmailReminders(ctx context.Context, query *EmailReminderQuery, nodes []*Tournament, init func(*Tournament), assign func(*Tournament, *EmailReminder)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Tournament)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.EmailReminder(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(tournament.EmailRemindersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.tournament_email_reminders
		if fk == nil {
			return fmt.Errorf(`foreign-key "tournament_email_reminders" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "tournament_email_reminders" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *TournamentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *TournamentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tournament.Table, tournament.Columns, sqlgraph.NewFieldSpec(tournament.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tournament.FieldID)
		for i := range fields {
			if fields[i] != tournament.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *TournamentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(tournament.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = tournament.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TournamentGroupBy is the group-by builder for Tournament entities.
type TournamentGroupBy struct {
	selector
	build *TournamentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *TournamentGroupBy) Aggregate(fns ...AggregateFunc) *TournamentGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *TournamentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TournamentQuery, *TournamentGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *TournamentGroupBy) sqlScan(ctx context.Context, root *TournamentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TournamentSelect is the builder for selecting fields of Tournament entities.
type TournamentSelect struct {
	*TournamentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *TournamentSelect) Aggregate(fns ...AggregateFunc) *TournamentSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *TournamentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TournamentQuery, *TournamentSelect](ctx, _s.TournamentQuery, _s, _s.inters, v)
}

func (_s *TournamentSelect) sqlScan(ctx context.Context, root *TournamentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
